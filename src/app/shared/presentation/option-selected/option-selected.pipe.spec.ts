import { OptionSelectedPipe } from './option-selected.pipe';

describe('OptionSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
