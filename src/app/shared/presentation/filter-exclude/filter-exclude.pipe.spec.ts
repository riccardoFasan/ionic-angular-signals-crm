import { FilterExcludePipe } from './filter-exclude.pipe';

describe('FilterExcludePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExcludePipe();
    expect(pipe).toBeTruthy();
  });
});
