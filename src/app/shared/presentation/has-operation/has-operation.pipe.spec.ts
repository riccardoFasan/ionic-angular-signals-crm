import { HasOperationPipe } from './has-operation.pipe';

describe('HasOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new HasOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
