import { TagsHandlerDirective } from './tags-handler.directive';

describe('TagsHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new TagsHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
