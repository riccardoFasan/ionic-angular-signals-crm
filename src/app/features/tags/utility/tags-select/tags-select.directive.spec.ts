import { TagsSelectDirective } from './tags-select.directive';

describe('TagsSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TagsSelectDirective();
    expect(directive).toBeTruthy();
  });
});
