import { DiaryEventIconPipe } from './diary-event-icon.pipe';

describe('DiaryEventIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaryEventIconPipe();
    expect(pipe).toBeTruthy();
  });
});
