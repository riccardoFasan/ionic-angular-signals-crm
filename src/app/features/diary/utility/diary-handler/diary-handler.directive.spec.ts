import { DiaryHandlerDirective } from './diary-handler.directive';

describe('DiaryHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new DiaryHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
