import { MealsHandlerDirective } from './meals-handler.directive';

describe('MealsHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new MealsHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
