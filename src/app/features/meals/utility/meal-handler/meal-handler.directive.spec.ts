import { MealHandlerDirective } from './meal-handler.directive';

describe('MealHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new MealHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
