import { ActivityTypesHandlerDirective } from './activity-types-handler.directive';

describe('ActivityTypesHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivityTypesHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
