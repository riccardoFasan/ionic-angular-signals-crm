import { ActivityTypesSelectDirective } from './activity-types-select.directive';

describe('ActivityTypesSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivityTypesSelectDirective();
    expect(directive).toBeTruthy();
  });
});
