import { ActivitiesHandlerDirective } from './activities-handler.directive';

describe('ActivitiesHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivitiesHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
