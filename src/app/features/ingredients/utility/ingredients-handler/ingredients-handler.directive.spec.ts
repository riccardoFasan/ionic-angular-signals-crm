import { IngredientsHandlerDirective } from './ingredients-handler.directive';

describe('IngredientsHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new IngredientsHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
