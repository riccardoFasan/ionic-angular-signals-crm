import { IngredientFoodsHandlerDirective } from './ingredient-foods-handler.directive';

describe('IngredientFoodsHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new IngredientFoodsHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
