import { IngredientsSelectDirective } from './ingredients-select.directive';

describe('IngredientsSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new IngredientsSelectDirective();
    expect(directive).toBeTruthy();
  });
});
