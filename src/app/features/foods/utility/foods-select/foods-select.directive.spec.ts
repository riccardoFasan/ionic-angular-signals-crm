import { FoodsSelectDirective } from './foods-select.directive';

describe('FoodsSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new FoodsSelectDirective();
    expect(directive).toBeTruthy();
  });
});
