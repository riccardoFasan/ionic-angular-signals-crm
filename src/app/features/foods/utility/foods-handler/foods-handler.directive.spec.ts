import { FoodsHandlerDirective } from './foods-handler.directive';

describe('FoodsHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new FoodsHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
